diff --git a/node_modules/react-native-worklets/android/build.gradle b/node_modules/react-native-worklets/android/build.gradle
index e189e1c..72ca8e6 100644
--- a/node_modules/react-native-worklets/android/build.gradle
+++ b/node_modules/react-native-worklets/android/build.gradle
@@ -3,6 +3,11 @@ import groovy.json.JsonSlurper
 import org.apache.tools.ant.taskdefs.condition.Os
 import javax.inject.Inject
 
+def localProps = new Properties()
+def localPropsFile = new File(rootProject.projectDir, "local.properties")
+if (localPropsFile.exists()) { localPropsFile.withInputStream { localProps.load(it) } }
+def nodeBinary = localProps.getProperty("NODE_BINARY", "node")
+
 def safeExtGet(prop, fallback) {
     rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
 }
@@ -34,7 +39,7 @@ def resolveReactNativeDirectory() {
     // Fallback to node resolver for custom directory structures like monorepos.
     def reactNativePackage = file(providers.exec {
         workingDir(rootDir)
-        commandLine("node", "--print", "require.resolve('react-native/package.json')")
+        commandLine(nodeBinary, "--print", "require.resolve('react-native/package.json')")
     }.standardOutput.asText.get().trim())
     if (reactNativePackage.exists()) {
         return reactNativePackage.parentFile
@@ -319,7 +324,7 @@ android {
 
 def validateReactNativeVersionResult = providers.exec {
     workingDir(projectDir.path)
-    commandLine("node", "./../scripts/validate-react-native-version.js", REACT_NATIVE_VERSION.toString())
+    commandLine(nodeBinary, "./../scripts/validate-react-native-version.js", REACT_NATIVE_VERSION.toString())
     ignoreExitValue = true
 }
 
