# KAN-455 — promoting the reviewed decisions to production

Prepared 2026-09-18 against `develop` @ `dd47682`. Every step below writes to
production and is run by Olegário, or by the agent only after "run step N"
in a message. Nothing here has been run.

## What production has today (read-only probe, 2026-09-18)

| thing | state | how it was checked |
| --- | --- | --- |
| Worker | last deployed **2026-09-06 15:11 UTC** (`wrangler deployments list`) — before KAN-444, 446, 447, 449, 450, 451, 452 touched `cloudflare/src` (18 commits) | `git log --since=2026-09-06T15:12 -- cloudflare/src` |
| Container image | same deploy; 29 extraction commits since, including the KAN-448 and KAN-457 promotion rules | `wrangler deploy` builds `extraction/Dockerfile` with the Worker |
| `0040_overture_evidence_run.sql` | **not applied** — `overture_evidence_run` table absent | `sqlite_master` |
| `0041_chain_brands_decide_kinds.sql` | **not applied** — 20/20 sampled retyped ids still `bus`/`phone_repair`/`beach`/`school` | `overture_poi` by id |
| `0042_curated_poi_provenance.sql` | **not applied** — no `origin_*` columns, no `idx_curated_poi_origin` | `PRAGMA table_info(curated_poi)` |
| `0043_chain_discovery_mapped_kind.sql` | **not applied** — Ale-Hop Faro is a `florist`, Rua do Ouro unbranded | `overture_poi` by id |
| Override batches | **9 of 94 promoted** (the first nine: `stationery` … `pet`). All 24 KAN-432 `reviewed_*`, all 62 `kan444_*`, all 11 `evidence_20260914T0200Z_*` are `pending` | `promote_override_batches.py status` as it was on 2026-09-18 (5 sampled ids per batch, ≤150 per query). The helper now reads every id of every batch; re-run `status` before step 3 to confirm the count with all ids |
| Reversals | all 33 withdrawn ids are `pending` and not in `overture_poi` — nothing to undo | `promote_override_batches.py status --reversals` |
| `type_relation` | 61 rows | count |
| One override id | `d6d19c2f-54d4-4ad6-91d3-76134e9e610e` (`reviewed_exclusions_professional`) is not in `overture_candidate` at all; the batch runs for its other 6 ids | status output |

Endpoint before-state: `docs/kan-455/verification-before-2026-09-18.json`
(table at the end of this file).

## Steps, in order

Work from `cloudflare/` on `develop` @ `dd47682` (or the release cut from
it — Olegário's call; every change below is on `develop`, `main` is still
v0.19.4). Docker must be running for step 1. `$SECRET` is the Worker's
`BUILD_TRIGGER_SECRET`; it is not in the repo or `.dev.vars`.

### Step 1 — deploy the Worker and rebuild the container (one command)

```
cd cloudflare && npx wrangler deploy
```

`wrangler.jsonc` lists the container, so this builds `extraction/Dockerfile`
and pushes the image with the Worker. Expect a new entry in
`npx wrangler deployments list`.

Check — the KAN-446 endpoint exists (400 = invalid payload on the new
Worker; the old one answered 404 for the path; `{}` writes nothing):

```
curl -s -o /dev/null -w '%{http_code}\n' -X POST https://poi-api.brushaway.app/internal/overture-country/evidence-run \
  -H "X-Build-Secret: $SECRET" -H 'User-Agent: curl/8.0' -H 'Content-Type: application/json' -d '{}'
```

`/internal/*` is guarded globally, so without the secret every path is 401
and proves nothing.

### Step 2 — migrations, in this order

The tracker is not in use; each file is applied with `d1 execute --file`
and checked by querying. 0040, 0041 and 0043 are idempotent; **0042 is not** — its `ALTER TABLE … ADD COLUMN` lines fail with "duplicate column" on a second run, so apply it exactly once and check first that `curated_poi` has no `origin_source` column.

> **STOP unless every one of the seven 0041 retypes has a recorded owner
> decision.** 0041 writes 30 primary-type changes; seven of them are
> suspect (decision 3 below): `Cafetaria LIDL Sesimbra`,
> `Papelaria/Tabacaria Intermarché De Palmela`, `Papelaria Intermarché
> Alfena`, `Snack Bar O Celeiro`, `Restaurante "O Celeiro" / Loja da Ana`,
> `Cafetaria "Tempo Caffé - Supermercado Auchan"`, `Atlântico pizzaria S.
> FÉLIX`. Before the 0041 line below runs, each of the seven must be written
> against one of these, in this file or on the PR:
>
> - **apply as reviewed** — 0041 goes in unchanged; the KAN-448 follow-up
>   (brand must lead the name on `BRAND_TYPES_FOR_OVERRIDE`) is filed;
> - **regenerate 0041** with the brand-leads-name rule — the rule change
>   lands first, 0041 is regenerated and re-read, and this runbook's row
>   counts for 0041 are updated;
> - **exclude by id** — the row's statements are removed from 0041 by
>   `overture_id`, and the id is listed here.
>
> A retype with no decision is an unresolved one. Do not apply 0041 with any
> of the seven unresolved, and do not apply 0043 (it assumes 0041). 0040 and
> 0042 do not depend on this and may go first. The answers are the owner's,
> not the runner's.
>
> Decisions recorded: _(none yet — fill in before step 2)_

```
npx wrangler d1 execute brush-poi-registry --remote --file=migrations/0040_overture_evidence_run.sql
npx wrangler d1 execute brush-poi-registry --remote --file=migrations/0041_chain_brands_decide_kinds.sql
npx wrangler d1 execute brush-poi-registry --remote --file=migrations/0042_curated_poi_provenance.sql
npx wrangler d1 execute brush-poi-registry --remote --file=migrations/0043_chain_discovery_mapped_kind.sql
```

| file | statements | rows | check |
| --- | --- | --- | --- |
| 0040 | 1 table | — | `SELECT name FROM sqlite_master WHERE name='overture_evidence_run'` → 1 row |
| 0041 | 4,587 | 1,887 | `verify_prod_decisions.py`: `decathlon_albufeira` and `benfica_official_store_jardim_regedor` pass |
| 0042 | 5 `ALTER TABLE`, 1 unique index, 2 `UPDATE`s backfilling the known-origin rows | 294 (per KAN-452) | `PRAGMA table_info(curated_poi)` shows `origin_source`, `origin_id`; `SELECT name FROM sqlite_master WHERE name='idx_curated_poi_origin'` |
| 0043 | 72 | 22 | `verify_prod_decisions.py`: `ale_hop_rua_do_ouro` and `ale_hop_faro` pass |

0043 assumes 0041 (it names only what 0041 does not). The gate above is
what "read the seven retypes" means: read, decide, record, then apply.

### Step 3 — the 85 pending override batches

Each batch is one container run of `overture-overrides` mode, triggered by
`POST /internal/overture-country/overrides {"countryCode":"PT","batch":…}`.
The Worker requires the PT import to be `mapped` with an immutable source
(it is). The helper triggers one batch, polls prod until **every id of the
batch** has left `pending` (all ids, in lists of ≤150 per read, 20 s poll,
10 min ceiling), then the next; a batch that does not settle stops the run
and prints the ids still pending.

```
cd cloudflare/extraction
python3 promote_override_batches.py status            # read-only, what is pending now
BUILD_TRIGGER_SECRET=$SECRET python3 promote_override_batches.py run --all-pending
```

Or one at a time: `… run reviewed_sports`. Order between batches does not
matter — no id is in two batches (the validator forbids it) and every batch
writes `INSERT OR IGNORE` plus a status update on its own ids.

Check: `python3 promote_override_batches.py status` → 94 `promoted`, 0
`pending`, 0 `mixed` — **all ids verified**: `status` reads every id of
every batch, so a `promoted` line means no id of that batch is pending,
not that a sample of it is. Then the endpoint at a promoted row, e.g. the
`evidence_20260914T0200Z_*` ids in `docs/evidence/PT/20260914T0200Z/overrides-draft.json`.

Reversals need nothing: the 33 withdrawn ids are out of their batches and
were never served.

> **STOP unless the 65 rule-based promotions have an execution path and a
> completed status.** "94 promoted, 0 pending" covers the override batches
> only. The 65 rows the KAN-448 rule newly promotes from generic shopping
> (36 supermarkets, 12 banks, 17 stores; `minipreco_carvoeiro_generic_shopping`
> is one) are not in any batch and no step above runs them (decision 2
> below). Step 3 is complete — and step 5's after table may be committed —
> only when both hold:
>
> 1. **an execution path is recorded** for the 65: the owner has chosen
>    one of the options in decision 2 (an `overture-repromote` mode over
>    `pending` for the current source key, or waiting for KAN-456's refresh)
>    and it exists — merged, deployed in step 1 or a later deploy, and named
>    here;
> 2. **that path has run to completion**: the 65 ids are `promoted` in
>    `overture_candidate` (every id, ≤150 per read, as `status` does) and
>    `minipreco_carvoeiro_generic_shopping` passes in `verify_prod_decisions.py`.
>
> If either does not hold, stop here and report what is outstanding — the
> 65 stay `pending` and unserved, which is safe; it is not done. Do not
> report KAN-455 complete on the batch count alone.
>
> Execution path recorded: _(none yet — decision 2 is open)_

### Step 4 — post the KAN-446 residual metric (once, after step 1)

From `docs/evidence/PT/20260914T0200Z/`: `manifestSha256` is the SHA-256 of
`manifest.json` itself (`shasum -a 256 manifest.json`), `configSha256` and
`toolCommit` are from inside it, counts from `residual-report.json`
(4,141 = 14 + 0 + 4,127; 14 + 0 = 3 + 11 — the Worker checks both sums).

```
curl -s -X POST https://poi-api.brushaway.app/internal/overture-country/evidence-run \
  -H "X-Build-Secret: $SECRET" -H 'User-Agent: curl/8.0' -H 'Content-Type: application/json' -d '{
  "countryCode": "PT", "runId": "20260914T0200Z",
  "manifestSha256": "56840a4ea6d298ba5e4b67c7b430c3277376b5a48b396573175b77a46699a94b",
  "configSha256": "fd149153ddcb8ec15357157c238d0f2f3cc8c6cd39b5dfbbe55c8bb34389c7bd",
  "toolCommit": "cdefc902ba1f628816b98860d6568233e703a128",
  "residualRows": 4141, "verifiedRows": 14, "excludedRows": 0, "insufficientRows": 4127,
  "foursquareRows": 3, "osmRows": 11 }'
```

Expect `201 {"ok":true}`; a repeat is `409 already recorded`. Needs 0040.

### Step 5 — verify and commit the after table

```
python3 cloudflare/extraction/verify_prod_decisions.py \
  --before docs/kan-455/verification-before-2026-09-18.json \
  --out docs/kan-455/verification-after-<date>.json
```

Commit the after file and paste the table into the PR.

## What the ticket did not anticipate — decisions for Olegário

1. **The "Casa …" rows.** Prod serves **329** generic-shopping rows named
   `Casa …` as `store`/`home` (padded brand match, KAN-448 rule 5 stops it
   for new promotions). Nothing above touches them: 0041 only writes rows
   the *chain* rule decides, and these are now `pending` under the rule,
   which is not a chain decision. `Casa do Rum` itself is already
   `wine_and_spirits` in prod (its `liquor_store` category kept its kind),
   so the ticket's check passes today; the damage is the 329
   (`casa_das_dores_generic_shopping` in the table is one). Options, both
   by exact id, both a separate reviewed migration: (a) hide them through
   `poi_source_correction` (KAN-452's removal path — no row deleted); (b)
   `DELETE` them from `overture_poi`/`_type`/`_attribute` and set the
   candidate back to `pending`. (b) is a stop-and-ask under ground rule 3.
   A further 74 `Casa …` rows served as `home` are `home` under the current
   rule too (the category said so) and are fine.
2. **The 65 promotions have no path.** The rows the KAN-448 rule newly
   promotes from generic shopping (36 supermarkets, 12 banks, 17 stores;
   `minipreco_carvoeiro_generic_shopping` is one) are `pending` in prod and
   no step re-runs promotion over pending rows: `overture-overrides` runs
   only listed ids, and re-queuing `/internal/overture-country/queue` on a
   `mapped` country clears `raw_extract_r2_key` and re-extracts Overture —
   that is KAN-456's refresh, not this. Either a small `overture-repromote`
   mode (run `run_country` over `pending` for the current source key — the
   writes are already `INSERT OR IGNORE` and status-guarded) or wait for
   KAN-456. Note also: 289 of Minipreço's 292 rows are `discount_store`,
   a commercial category the brand rule does not overrule, so they stay
   `store`/`any` under every step here; only generic-shopping Minipreços
   become supermarkets.
3. **0041 — seven retypes to look at before applying.** Of its 30 primary-type
   changes, seven come from the non-store brand override on rows with an
   *empty* category, where `find_brand` does not require the brand to lead
   the name: `Cafetaria LIDL Sesimbra` → supermarket Lidl; `Papelaria/Tabacaria
   Intermarché De Palmela` and `Papelaria Intermarché Alfena` → supermarket;
   `Snack Bar O Celeiro` and `Restaurante "O Celeiro" / Loja da Ana` →
   supermarket Celeiro; `Cafetaria "Tempo Caffé - Supermercado Auchan"` →
   supermarket; `Atlântico pizzaria S. FÉLIX` → **bank** ATLANTICO. The other
   23 (FNAC ×13 out of `shopping_center`/`parking`, Decathlon ×3 out of
   `school`/`beach`, Lacoste out of `school`, Worten Mobile, Opticalia ×3)
   are right. Apply as reviewed and file a KAN-448 follow-up (require the
   brand to lead the name on the `BRAND_TYPES_FOR_OVERRIDE` path, as the
   store path already does), fix first and regenerate 0041, or exclude the
   seven by id — Olegário's call, recorded per row in the step 2 gate
   before 0041 runs; the rule change is one line and a regenerated file.
4. **KAN-432's 24 `reviewed_*` batches were never promoted either.** The
   ticket lists `kan444_*` and `evidence_*`; step 3 runs all 85 pending.
5. **There is no "KAN-447 club-store batch."** Club stores came through the
   dictionary and the chain rule; their prod correction is inside 0041
   (`club_store` inserts for Benfica/Sporting/Porto official stores).
6. **No Benfica Official Store at Estádio da Luz in the archive.** The
   check uses `Benfica Official Store Jardim Regedor` (Lisboa, in 0041).
7. **The Worker is 12 days and 18 `cloudflare/src` commits behind** — step 1
   ships KAN-444 through KAN-452 together (`/internal/r2/list`, evidence-run,
   moderation lookups over Overture, on-demand Overture Places, KAN-451
   place identity). Deploying from `develop` mid-sprint is a departure from
   "main is production"; say if you want a release cut first.

## Before table (2026-09-18, endpoint)

| check | now | result | by |
| --- | --- | --- | --- |
| decathlon_albufeira | `school`, no kinds | FAIL | 0041 |
| casa_do_rum_porto_da_cruz | `store`/`wine_and_spirits` | PASS | already right |
| casa_das_dores_generic_shopping | `store`/`home` | FAIL | decision 1 |
| minipreco_carvoeiro_generic_shopping | absent | FAIL | decision 2 |
| benfica_official_store_jardim_regedor | `store`/`sports` | FAIL | 0041 |
| ale_hop_rua_do_ouro | `store`/`gift`, brand null | FAIL (brand) | 0043 |
| ale_hop_faro | `florist` | FAIL | 0043 |
| Odivelas Parque 200 m, 125 types | 110 rows; 12 store brands, none with inconsistent kinds | snapshot | — |
