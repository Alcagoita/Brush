// Jest stub for `require('*.tflite')` — the real asset is a binary handled by
// Metro at build time. Tests mock react-native-fast-tflite, so the value here
// is only a placeholder module id.
module.exports = 1;
