# Generated by generate_dataset.py — regenerable, not committed.
train.csv
validation.csv
# Training outputs land in modules/brush-poi-classifier (native asset) + the JS
# layer, not here. Ignore stray copies dropped in this dir.
model.tflite
vocab.json
labels.json
exported_model/
